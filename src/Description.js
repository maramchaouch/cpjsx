
import React from "react";

const Description = ({ description }) => {
  return <p style={{ textAlign: "center" , fontWeight: "bold", fontSize: "15px",color: "blue"}}>{description}</p>;
};

export default Description;
